<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CarouselMapper">

    <select id="selectAll" resultType="com.example.entity.Carousel">
        select carousel.*, article.title as articleTitle from `carousel`
        left join article on carousel.article_id = article.id
        <where>
            <if test="articleTitle != null"> and article.title like concat('%', #{articleTitle}, '%')</if>
        </where>
        order by id desc
    </select>

    <delete id="deleteById">
        delete from `carousel`
        where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.Carousel" useGeneratedKeys="true">
        insert into `carousel`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="img != null">img,</if>
            <if test="articleId != null">article_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="img != null">#{img},</if>
            <if test="articleId != null">#{articleId},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.Carousel">
        update `carousel`
        <set>
            <if test="img != null">
                img = #{img},
            </if>
            <if test="articleId != null">
                article_id = #{articleId},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>